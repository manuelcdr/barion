import { FiltroSimplesPipe } from './filtro-simples.pipe';

describe('FiltroSimplesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSimplesPipe();
    expect(pipe).toBeTruthy();
  });
});
